apiVersion: v1
kind: Service
metadata:
  name: kubernetes-bootcamp-service
spec:
  type: NodePort
  selector:
    app: kubernetes-bootcamp # TO COMPLETE
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 8080  